# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'yard-companion' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for yard-companion
  pod 'Firebase/Core'

  target 'yard-companionTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'yard-companionUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
